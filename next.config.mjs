/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['firebasestorage.googleapis.com', 'maps.googleapis.com'], // Add the Google Maps API domain
    },
  };
  
  export default nextConfig;
  